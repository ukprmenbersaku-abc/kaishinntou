// Gemini APIの使用は停止されました。
// このファイルは、将来的な機能拡張のためにプレースホルダーとして残されています。

export const sendMessageToGemini = async (
  message: string,
  history: any[]
): Promise<string> => {
  return "現在、AI機能は無効化されています。";
};